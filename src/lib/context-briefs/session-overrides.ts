/** Session-level overrides when automatic week/track mapping is insufficient. */
export const contextBriefSessionOverrides: Record<string, string | null> = {
  "claude-code-floater-session": null,
};
